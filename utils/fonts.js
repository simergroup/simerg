import { Roboto, Oxanium } from "next/font/google";

export const roboto = Roboto({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const oxanium = Oxanium({
  subsets: ["latin"],
  display: "swap",
});
